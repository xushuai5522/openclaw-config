# 2026-03-22

## 人人租自动运营系统收口（11:30-12:00）
- 大哥明确要求：不要继续重复踩坑，要把旧执行逻辑收口成最新长期工作流。
- 已新增项目文档：
  - `projects/rrz-workflow-v1.md`：明确系统边界、四层架构、v1 交付物、实施顺序。
  - `projects/rrz-blockers.md`：整理当前阻塞点，按 P0/P1/P2 分层。
- 已更新项目卡 `projects/rrz.md`：
  - 当前主阻塞改为“Vue 组件定位脆弱 + 原子提交脚本缺失”；
  - 执行策略强制默认走 SDK 注入上传 + Vue 数据层写回；
  - 明确禁止将素材中心 / file input 当默认自动化路线。
- 已更新 `skills/rrz-e2e/SKILL.md`：
  - 图片上传改为“默认且唯一主路线：`rrz_upload.js` + `createUploader` + Vue 数据层”；
  - 新增强约束：禁止依赖固定 Vue uid，必须按特征动态定位；
  - 记录后续新增脚本：`rrz_vue_locate.js` / `rrz_submit_atomic.js` / `rrz_capture_state.js`。
- 已更新 `registry/playbooks.json`：
  - 人人租相关剧本不再允许默认走素材中心 / file input；
  - 派发子代理时必须带上最新路线与阻塞清单；
  - 避免沿用旧上传逻辑重复踩坑。
- 已更新 `MEMORY.md`：
  - 新增长期教训：人人租执行器禁止依赖固定 Vue 组件 uid，必须按特征动态定位并优先收敛为原子提交脚本；
  - 人人租项目索引加入 `projects/rrz-workflow-v1.md` 与 `projects/rrz-blockers.md`。
- 已新增 3 个执行器骨架脚本：
  - `skills/rrz-e2e/scripts/rrz_vue_locate.js`
  - `skills/rrz-e2e/scripts/rrz_submit_atomic.js`
  - `skills/rrz-e2e/scripts/rrz_capture_state.js`

## 关键结论
- 之前重复踩坑的根因，不是单纯不会做，而是“旧逻辑没有被清理/降级，正确路线没有被强制写死”。
- 从今天开始，人人租自动运营默认路线明确为：
  `标准商品输入 -> 商品包 -> iframe 注入上传 -> Vue 数据层写回 -> 草稿/提审 -> 列表回查`。
- 814346（MDO3052）继续作为 v1 样板单，后续优先验证动态组件定位 + 原子提交脚本闭环。
- 大哥进一步明确了系统定位：**人人租上架 + 商家后台运营是长期任务、长期主方向**；以后应按长期维护目标和持续升级工作流来处理，不再按一次性项目思路推进。
- 样板单 `814346`（MDO3052）已在 12:52 左右真实跑通到“审核中”：
  - 动态 Vue 定位成功
  - OSS SDK 图片上传成功
  - 主图/描述图写回成功
  - 关键修正：图片对象必须补 `src=url`
  - 真实 `saveData/submitData` 链成功
  - 列表回查状态由“草稿”变为“审核中”
- 该结论已提升为长期记忆：后续人人租执行器默认沿“动态定位 + SDK 上传 + 补 src + saveData/submitData + 列表回查”主链执行。  
