# 2026-03-14 每日记录

## 主要事件
- 执行每日复盘与记忆维护任务，并基于今天已记录事项整理长期记忆。
- 今天的高价值信息主要集中在两块：人人租项目执行口径收敛、OpenClaw / session 排障修复。

## 人人租项目更新
- 大哥确认：**自动登录功能已经完成**。
- 后续推进人人租项目时，默认前提改为：**自动登录已可用/已完成**，不要再把“登录能力缺失”当成主问题反复排查。
- 若再次遇到登录页，优先判断为：
  1. 登录态过期；
  2. 风控触发；
  3. 页面跳转/会话异常；
  而不是“自动登录功能未完成”。
- 项目工作中需要**长期记住**这条事实，后续梳理、排障、汇报都按此口径执行。
- 2026-03-14 18:30 大哥再次强调：人人租账号密码已在项目文件中，遇到登录需求应由我**自行完成登录**；以后不要再要求大哥手动登录或重复确认这项功能。

## 人人租示波器上架进展
- 大哥最新要求：当前优先完成**示波器**上架，不再继续 Mac mini。
- 已确认示波器资料目录：`products/MDO3052/`
  - 核心文件：`product_info.json`、`upload_data.json`
  - 图片素材：`mdo3052_2.jpg`、`mdo3052_5.png`、`mdo3052_6.png` 等。
- 已确认示波器商品文案与价格基线：
  - 标题：`9成新 泰克 MDO3052 混合域示波器 实验室调试信号分析`
  - 设备估值：40000
  - 押金：32000
  - 关键租金：7天3600、1个月6000、3个月17100、6个月32400、1年61200
- 当前在人人租后台已切换到类目：`检测仪器 > 仪器测试设备`，并进入添加商品页。
- 该类目下**没有现成“泰克 / MDO3052”品牌型号**，已尝试走“未找到需要品牌/型号，点击申请”流程。
- 新发现的稳定问题：品牌型号申请弹窗同样存在前端/Vue 绑定不稳定，普通 fill 不一定能写入，需要优先使用真实逐字输入或底层状态写入方案。
- 关机前现场断点：人人租后台已登录；示波器发布页已切到 `检测仪器 > 仪器测试设备`；品牌型号申请弹窗已打开，目标为提交 `泰克 / MDO3052`；重启后应从该弹窗/该商品线继续，而不是回到 Mac mini。
- 大哥新增执行偏好：**独立任务优先安排子代理处理，主代理以检查、协调、验收为主**。后续遇到可拆分、耗时、独立推进的任务，默认先派子代理。

## OpenClaw / Session 排障
- 21:32 左右，群里反馈有 session 任务执行异常，已按 `openclaw-doctor` 思路排查。
- `openclaw status` 显示 Gateway 正常、当前群聊主会话正常，问题不在主服务整体不可用。
- 根因证据来自 `~/.openclaw/logs/codex-local.err.log` / `codex-local.log`：旧的 codex-local 会话反复出现 `subprocess.TimeoutExpired`、`timed out after 180 seconds`、`BrokenPipeError`，并伴随大量 502/504。
- 本质判断：旧飞书私聊 session 累积了过厚历史上下文（heartbeat / bootstrap / 历史长消息），导致 codex-local 启动请求在 180 秒内处理不完。
- 已执行修复：
  1. 备份 `sessions.json`；
  2. 备份旧私聊 session 文件 `f7f7f1f8-ffb3-4cc2-b2a5-9bfda44fdbda.jsonl`；
  3. 将该 session 文件改名为 `*.reset-manual-*`；
  4. 从 `sessions.json` 中移除 `agent:main:feishu:direct:ou_8f96c8e59111d256baf4213e7480caaf` 映射，让后续私聊自动新建干净 session。
- 当前群聊 session 未动，避免影响正在进行的对话。
- 附带观察：`gateway.err.log` 里还有重复尝试拉起 gateway 的历史痕迹，但 `gateway-watchdog.log` 最新看起来已恢复正常，不是本次主因。

## 技术教训
- **会话异常不要先怀疑主服务全挂**：先区分是 Gateway 整体故障，还是某个旧 session 因上下文过厚而单点失效。
- **旧 session 过厚时，重置比硬拖更有效**：先备份 `sessions.json` 和对应 session 文件，再移除映射/重建干净 session，风险更可控。
- **人人租前端表单要警惕“看起来填上了，实际没写进去”**：涉及品牌/型号申请弹窗，优先真实逐字输入或更底层写值校验。

## 复盘结论
- 今天新增的长期有效信息主要有三条：
  1. 人人租登录能力已完成，后续应默认自行处理登录恢复；
  2. 可拆分、耗时、独立推进的任务，默认优先交给子代理；
  3. OpenClaw 某些异常本质是旧 session 上下文过厚，处理上应走“备份 → 断开旧映射 → 新建干净 session”的思路。
