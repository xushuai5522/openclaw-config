## 00:09 菜单系统关键偏好与规则（durable）
- 大哥明确要求：菜单系统要做成“可运行、数据驱动”的真实系统，不接受手写演示版。
- 菜品来源不只是“做法链接”，还要持续扩充“沭阳民间家常菜”到菜品库本体。
- 配图策略已明确：
  - 菜谱配图 **真图优先**（抖音/小红书/B站等真实内容）；
  - **禁止继续用 AI 生图做菜谱图**；
  - AI 生图仅用于“人人租机上架产品”场景。
- 菜单结构规则已明确：按“早/中/晚”设计；中晚饭允许重叠，但**午饭最多仅 1 道菜可在晚饭重叠**；晚饭采用“1 道午饭复热 + 1 道补菜”。
- 页面交付要求：要有可感知的布局变化，避免“改了但看不出来”；前端缓存可能导致误判，需要提供强刷/版本参数规避。

## 当前项目状态快照（durable）
- `projects/rrz-dashboard/meal_library.json` 已从 106 扩到 118（新增沭阳民间家常菜 12 道）。
- `projects/rrz-dashboard/home.py` 已接入多来源做法入口（抖音/小红书/B站/下厨房）与 `day_schedule`（早餐/午餐/晚餐）。
- 当前待持续工作重点：
  1) 继续扩沭阳民间家常菜库（目标 150+）；
  2) 菜谱图逐步替换为可追溯真图并记录来源；
  3) 前端布局继续做“强可见”优化，减少用户“页面没变化”的感知问题。

## 08:47 电商运营学习规则（durable）
- 大哥新增要求：每天收到的咨询/学习内容，都要沉淀为“学习总结 + 可执行动作”。
- 租赁电商 2026 开局策略先后顺序：
  1) 先提升搜索命中率（标题优先“场景词+型号词”）；
  2) 再做动态定价（基准价 × 库存系数 × 时段系数 × 风险系数）。
- 调价执行纪律：每周最多调一次系数，按“曝光→点击→下单”漏斗看两周再二轮优化。

## 18:08 菜品配图全量复核结果（durable）
- 按大哥要求对菜品库执行“每个菜都过一遍”的配图检查，并提供页面截图确认。
- 本轮复核结果：菜品库 107 道，图片清单 107 张，未出现“找不到图需下线”的条目。
- 复核中仅 1 道菜触发重找并替换成功：`沭阳鸡糕`。
- 当前口径：菜单/菜库页面可截图验收；后续可按更严格规则（菜名关键词命中来源标题）继续筛图。

## 23:00 每日复盘与记忆维护（执行策略固化）
- 本次复盘已按“先落 daily、后看 MEMORY”的顺序执行，确保 `memory/2026-03-17.md` 成功落盘作为主体完成标准。
- 今日新增可沉淀教训（流程层）：记忆维护任务里，`MEMORY.md` 仅在出现长期有效新原则时才最小改动；若精确编辑不稳定，应明确“建议更新但本次未改”，不因该步骤影响整单完成。
- 回看最近 3 天日记（03-15 / 03-16 / 03-17）后判断：当前长期原则集已覆盖今天的新增信息，本次 `MEMORY.md` 无需更新。

## 22:00-23:41 OpenClaw长任务架构与人人租推进（durable）
- 大哥明确决策：当前阶段“长任务是主任务”，原“心跳不做长任务”设计不合理；要求改成心跳仅保活/告警，不得中断长任务。
- 已落地规则变更：
  - `HEARTBEAT.md` 改为“心跳仅健康检查 + 阻塞告警”；
  - 新增“长任务并行规则”：长任务默认独立执行通道（子代理/子会话/后台），被外部打断后优先续跑同一任务；
  - `AGENTS.md` 新增“Long tasks are first-class default work”，并强调 heartbeat 不应节流主任务。
- 运行时配置已改为长任务模式并验证：
  - `~/.openclaw/openclaw.json` 中 `agents.defaults.timeoutSeconds=31536000`；
  - `plugins.entries.llm-task.config.timeoutMs=31536000000`；
  - gateway 重启后 `RPC probe: ok`。
- 多代理骨架已部署：`main / finance-butler / rrz-ops / deep-worker`，并创建独立 workspace：
  - `/Users/xs/.openclaw/workspace-rrz`
  - `/Users/xs/.openclaw/workspace-research`
- rrz 专项能力整合已落地：
  - 新建 `skills/rrz-e2e/SKILL.md`（设备价值定价→主图/描述图双上传→草稿/提审→列表回查）；
  - `registry/playbooks.json` 新增 `rrz-value-image-pricing-chain` 与 `rrz-auto-dispatch-ops`；
  - `projects/rrz.md` 增加“设备价值+图片素材串联能力”固定顺序。
- 子代理可用性验证通过：`rrz-ops` 能读取 `rrz-e2e` 并落盘 `/Users/xs/.openclaw/workspace-rrz/DEPLOY_CHECK_RRZ_OPS.md`（内容 `rrz-ops pipeline ready`）。
- 人人租商品（ID 812806）当晚推进结论：
  - 已在编辑页推进并部分填值（含押金/购买价等阶段性字段），但未成功提审；
  - 持续阻塞点是 iframe 内销售规格逐格写入稳定性（ref 失效/控件定位不稳）+ 图片/描述未完成导致前置校验未过；
  - 大哥明确要求后续“不要批量，单个填写”。
