# 2026-03-21

## Claude Code / OpenClaw 接入排障
- 检查 OpenClaw 当前模型配置：默认主模型为 `yescode-gpt/gpt-5.4`，已配置 `yescode-gpt/gpt-5.3-codex-spark` 和 `Claude-url/claude-opus-4-6-c` 作为可选模型。
- 当前会话 `session_status` 显示主模型 `yescode-gpt/gpt-5.4` 正在工作，说明主回复链路可用。
- 用本机 CLI 执行 `openclaw status --deep` / `sessions_spawn(runtime=subagent)` 时出现 `gateway token mismatch`，说明本地 CLI→Gateway 鉴权链路异常，后续如需命令行侧验证/修复要先处理该问题。
- 已为 Claude Code 写入用户级配置 `~/.claude/settings.json`。
- 调研官方最新文档后确认：Claude Code 支持第三方/网关，但要求网关兼容 Anthropic Messages / Bedrock / Vertex / Foundry 协议；不能直接把任意 OpenAI 兼容接口当成可用后端。
- 实测 OpenClaw 里的 `Claude-url` provider 不兼容 Claude Code：`/v1/messages/count_tokens` 缺失，`/v1/messages` 的模型路由也不正常，不能直接跑通。
- 已新增本地桥接脚本 `scripts/claude_yescode_bridge.py`，把 Claude Code 的 Anthropic Messages 请求桥接到 OpenClaw 现有的 `yescode-gpt/gpt-5.4`（OpenAI chat completions）。
- 真实验收通过：`claude -p --model opus` 能返回固定文本；并且能读取 workspace 内 `AGENTS.md` 文件并正确返回第一行，说明 Claude Code 基础 agent/tool 工作流已打通。

## 人人租任务派给 Claude Code 的实际阻塞（17:20-17:43）
- 大哥明确要求：把人人租上架推进任务派给 Claude Code 去做，验证它能否接管当前卡点。
- 已补齐人人租上下文：重新读取 `skills/rrz-e2e/SKILL.md`、`projects/rrz.md`、`registry/playbooks.json`，确认当前主线仍是“真图上传 → Vue 写入 → 草稿 → 提审 → 列表回查”闭环验证。
- 首次尝试 `sessions_spawn(runtime="acp")` 时暴露两层真实问题：
  1. 线程绑定在 Feishu 通道不可用；
  2. 更底层的 CLI/ACP 链路报 `gateway token mismatch`。
- 继续排查后定位到更具体根因：`127.0.0.1:18789` 上同时存在 OpenClaw 当前网关和 QClaw 遗留 `openclaw-gateway` 进程，CLI 连接时高概率打到旧进程，导致 token mismatch。
- 处理该端口冲突后，再次验证发现：`gateway token mismatch` 这一层已被打通，新的真实阻塞变成 `ACP runtime backend is not configured. Install and enable the acpx runtime plugin.`
- 进一步确认当前插件集只有 `feishu / llm-task / memory-core`，缺少 `acpx`，因此 Claude Code 仍无法通过 ACP 会话正式接活。
- 多次尝试执行安装命令 `/Users/xs/Documents/node-v24.14.0-darwin-x64/bin/openclaw plugins install acpx`，但都卡在 exec 审批链：Feishu 通道不能直接承接审批，异步审批多次超时/被拒，最终 `acpx` 当天未装上。
- 结论：今天不是 Claude Code 本体不行，而是 OpenClaw 本机 ACP 后端未安装 + 当前审批链打不通，导致无法把人人租任务正式派进去。

## 心跳 / 系统状态（晚间）
- 心跳检查读取 `HEARTBEAT.md` 并执行 `openclaw status`。
- 结果显示：主会话可用，但本地 Gateway 仍报 `unauthorized: gateway token mismatch`，且 `Gateway service` 状态为 `LaunchAgent installed · not loaded · unknown`。
- 另有 1 条持续性安全告警：Feishu doc create 可能授予文档权限；属于已知配置告警，非当天新增故障。

## 今日决策与教训
- 决策：优先先把 Claude Code 接入链路修通，再把人人租任务派过去；若链路不通，就不能把“派给 Claude Code”当成已完成。
- 技术教训 1：`gateway token mismatch` 不一定是 token 写错，也可能是本机存在旧网关/同端口残留进程抢占，必须先核对实际监听进程再判断配置。
- 技术教训 2：ACP 类能力是否可用，不能只看主回复链路；必须单独验证 `acpx` 等后端插件是否已安装、审批链是否可实际放行。
- 技术教训 3：在 Feishu 通道里遇到 exec 审批型安装任务，要尽早识别“口头同意 ≠ 审批落地”，避免重复空转。

## 记忆维护检查（23:00）
- 已回看最近 3 天日记：`memory/2026-03-18.md`、`memory/2026-03-19.md`、`memory/2026-03-20.md`。
- 本次优先保证 `memory/2026-03-21.md` 成功落盘，主体任务完成。
- 判断：今天新增内容主要是 Claude Code/ACP 接入排障细节与当日阻塞，长期原则层面仍可被现有 `MEMORY.md` 覆盖；`MEMORY.md` 本次无需更新。
