# 2026-03-15 每日记录（摘要版）

## 今日关键结论
- 已完成一版**轻量上下文管理机制**：`projects/*.md`、`registry/playbooks.json`、`recent/wins.jsonl`、`recent/failures.jsonl`、`scripts/context_recall.py` 已落地并验证可用。
- 大哥明确：**自我察觉、主动反思、持续进化** 是我的第一能力；发现忘上下文、没想起新能力、重复犯错、策略不够聪明时，必须先复盘、再固化、再验证。
- 主代理定位重新校准：以**交互、调度、监控、验收、上下文管理**为主；复杂且可拆分的具体执行优先交给子代理。
- 决策能力是当前主要短板；非紧急任务要先判断目标、路径、取舍，再干净执行。
- 新增执行偏好：遇到**已有工作流**的连续项目，先自动召回项目卡、注册表、recent 经验和产品目录，不要先反问大哥要资料。

## 人人租相关
- `DJI Pocket 3` 上架已推进到“添加商品”表单，类目可走：**摄影航拍 → 手持云台 → DJI/大疆**。
- 已产出 3 张可用主图：`product_images/DJI_Pocket_3_*_processed.jpg`。
- 当前核心卡点：**素材中心 / 自定义上传组件链路不稳定**。
- 重要结论：**upload 成功 ≠ 图片已挂到商品**；后续必须验证“素材中心回显 + 商品页图片区回显”。

## 系统排查结论
- 今晚的整机异常更像**软件触发关机/重启**，不是 kernel panic / GPU panic / WindowServer 崩溃。
- 高度嫌疑链路是 **UURemote**；普通 SSH 暂无直接证据。
- `ToDesk.app` 已移到废纸篓，但后台服务/驱动仍有残留，前台移除不等于彻底卸载。
- 晚间 heartbeat 补充发现：**Gateway RPC probe 正常不等于 LaunchAgent 正常**。当 `openclaw gateway status` 同时出现 `RPC probe: ok` 与 `Service unit not found / Service not installed` 时，应区分“网关进程仍在监听”与“服务托管未安装/未加载”两个层面，不要混为一谈。
- 今日还有一条失败信号：某次 `exec` 任务被 `SIGTERM` 杀掉，并伴随 `urllib3 v2 only supports OpenSSL 1.1.1+` 警告；目前更像具体任务/环境问题，不足以直接判定网关主服务异常。

## Cron 收口
- 控制台里一批 cron 失败多为旧残留或 delivery 修复后遗留噪音，不全是今天新炸。
- 后续查看 cron 时，若 gateway 异常，优先直接读 `~/.openclaw/cron/jobs.json` 与 `runs/*.jsonl`。

## 运行时新教训
- 长运行联调任务挂在主会话里，可能被系统 heartbeat / 新轮次打断并收到 `SIGTERM`；这不是“上下文又肥了”，而是执行模型冲突。
- 后续策略：长任务分三类处理——可拆的改短步；需持续跑的放独立执行通道/子代理；需大哥实时观察协同的任务用主线程短回合推进，不再长挂。
- 大哥明确要求：这条必须记住，避免以后再次让其他长任务无故中断。

## 备注
- 今天这份 daily note 已主动压缩为摘要；长过程、证据和专题细节不再默认堆在启动必读层。
