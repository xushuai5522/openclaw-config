# 2026-03-20

## rrz-e2e 加固落地（01:00-01:30）
- 大哥提出 6 条加固 + 6 个风险解决方案，全部落地到 `skills/rrz-e2e/`：
  - SKILL.md 重写：原子流程每步嵌入 runtime 调用点
  - `scripts/e2e_runtime.py`：商品锁(flock)、幂等 step_id、重试熔断(3次上限)、原子写(tmp→rename+哈希)、告警去重(5min)、动态超时规则
  - 验证通过：init → check → done → fail → alert 全链路 OK

## 图片上传突破（01:00-01:30）
- 逆向了人人租 OSS 上传 SDK（`rrzuOssSdkUmd.js` + `oss-sdk.js`）
- 完整上传链路：
  1. `GET https://go-micro.rrzu.com/file/oss/token?project=shop-admin&file_type=2` → AES 加密凭证
  2. SDK 用 cookie 密钥（`shop-admin_oss_token_key`）AES 解密
  3. 得到 `{host, policy, access_id, signature, dir, token, url}`
  4. `POST https://rrzu-backend.oss-cn-shanghai.aliyuncs.com` FormData 上传
  5. CDN 地址：`https://img1.rrzuji.cn/{path}`
- 实测上传成功：`https://img1.rrzuji.cn/shop-admin/images/20260320/3BAn98plhRyLfr5McUlvPwAzuAZ.jpg`
- 写了 `scripts/rrz_upload.js`：注入 iframe 后调用 `rrzUploadOne(b64)` 即可上传
- 这解决了之前卡了一个月的"图片上传组件无法自动化"问题
- 关键洞察：不需要操作 file input，直接用页面已加载的 SDK + base64 转 File 对象即可

## 教训（09:09）
- 会话重建后读到了 memory 却没直接衔接执行，反而重复问大哥要已有信息
- 正确做法：读完 memory → 识别断点 → 直接从待做第一项开始干
- 违反了"先自己找答案再问"原则，浪费大哥时间

## 待做
- [ ] 用真实商品图片跑一次完整 e2e 流程验证
- [ ] 上传后写入 Vue 数据层（picList / descPicList）验证
- [ ] 提审 → 列表回查闭环

## 当日新增记录（晚间补记）

### 人人租审核巡检（08:00）
- 按 `rrz` 技能流程巡检“审核不通过”商品列表，结果为空。
- 结论：当前店铺没有被打回、需要自动修复再提审的商品，当时店铺状态正常。

### Git 自动同步 / 心跳（14:03）
- Git 自动同步检测到 `openclaw-config` 有 66 个文件变更并已提交。
- 心跳会话里出现模型侧异常：`kiro` token 处于 cooldown / account_suspended，但最终心跳仍返回 `HEARTBEAT_OK`。
- 说明：告警来自模型通道而非 Git 提交本身，系统保活链路当时仍可继续工作。

### qclaw 安装判断（22:13-22:54）
- 大哥要求安装“腾讯发布的 ai agent qclaw，可接入微信”。
- 实查结果：
  - `web_search` 因缺少 Brave API key 无法使用；
  - 用户浏览器直连失败（Chrome `DevToolsActivePort` 不存在）；
  - `npm search/view` 查到的 `qclaw-cli` 指向社区仓库 `Sobranier/openclaw-cli`，readme 明确写明只是 `openclaw-cli` 的 alias wrapper，不足以判定为“腾讯官方方案”。
- 已据此选择不直接安装，转为向大哥索要官方仓库/文档链接，避免把社区壳包误装成官方微信接入方案。

### 今日教训（新增）
- 遇到“官方产品/官方接入”类安装请求，若来源无法核验，宁可停在核实阶段，也不要把社区 alias 包当官方方案落地。
- 外部检索链路有依赖前提：缺 Brave key、用户 Chrome 未就绪时，网页核验能力会受限，需要及时切换到 `npm view` / 仓库元数据等可验证来源。

### 23:00 每日复盘与记忆维护
- 已回看今日会话与最近 3 天日记（03-18 / 03-19 / 03-20）。
- 本次优先保证 `memory/2026-03-20.md` 成功落盘；主体任务完成。
- 复盘结论：今日新增高价值记录主要是“人人租审核巡检为空”“Git 自动同步 + 心跳异常但保活成功”“qclaw 官方性未核实因此未安装”。
- 长期记忆判断：本次已补充 2 条长期原则到 `MEMORY.md`：
  1) 会话重建 / 读完 memory 后要直接衔接断点，不重复问已知信息；
  2) “官方产品/官方接入”类请求先核验来源，无法证实时停在核实阶段。
