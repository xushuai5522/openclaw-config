# 2026-03-24

## 图片处理流程推进（09:00）
- 新增 `skills/rrz-publish/scripts/image_pipeline_runner.py`
  - 输入：`product_info.json` + 商品目录原始图 / `raw/`
  - 输出：`processed/`、`main/`、`desc/`、`image_pipeline_result.json`
  - 主图处理：白底、居中、统一 800x800
  - 描述图处理：标准化为 JPG，限制最大宽度
  - 支持 `image_pipeline_request.json` 显式指定 `main_images` / `desc_images`
  - `upload_payload` 已输出，方便后续给执行器直接消费
- 用 `products/MDO3052` 实跑验证通过：
  - 产出 `main/main_01.jpg` ~ `main_05.jpg`
  - 产出 `desc/desc_01.jpg`
  - 产出 `image_pipeline_result.json`
- 发现坏素材：`raw/mdo3052_official.jpg` 非可识别图片；runner 已加“坏图自动跳过并记入 invalid_images”，避免单张坏图阻断整条链路。

## 审核员 V1 落地（09:07）
- 新增 `skills/rrz-publish/scripts/audit_reviewer.py`
  - 输入：`product_info.json`、`upload_data.json`、`image_pipeline_result.json`
  - 输出：`audit_result.json`
  - 当前实现：标题/品牌/型号/禁词/套餐模板/押金/主图描述图数量与尺寸/坏素材告警
- 用 `MDO3052` 实跑：结果 `warning`，分数 92，阻断项 0，告警项 2。

## 提审链正式接线（09:10）
- 新增 `skills/rrz-publish/scripts/build_submit_payload.py`
  - 从 `product_info.json` + `upload_data.json` + `image_pipeline_result.json` + `audit_result.json` 组装 `submit_payload.json`
  - 输出字段已直接对齐 `rrzSubmitAtomic`：`title/details/mainImages/descImages/sellTableData/auditResult/auditStatus`
- 修改 `skills/rrz-e2e/scripts/rrz_submit_atomic.js`
  - submit 前强制读取 `payload.auditResult / auditStatus`
  - 若 audit 为 `block` 或存在 `blocking_issues`，直接阻断 submit
  - 若 audit 为 `warning`，默认允许 submit；可通过 `allowSubmitOnWarning=false` 改成 warning 也阻断
  - 若要求先审（`requireAuditBeforeSubmit=true`）但 payload 缺审核结果，也阻断 submit
- 链路已打通为：
  `image_pipeline_runner.py -> audit_reviewer.py -> build_submit_payload.py -> rrz_submit_atomic.js`

## 审核员升级为“参数型审核员”（09:17）
- 新增 `skills/rrz-publish/scripts/raw_auditor.py`
  - 输入：`raw/` 或商品目录原始图 + `product_info.json`
  - 输出：`raw_audit_result.json` + `image_pipeline_request.json`
  - 能做：坏图剔除、低分辨率素材降级、主图/描述图候选推荐、直接输出图片处理参数
- 升级 `image_pipeline_runner.py`
  - 真实消费 `image_pipeline_request.json`
  - 支持 `drop_sources`、显式 `main_images/desc_images`、`reuse_main_images_as_desc`
  - 输出 `request_used`
- 升级 `audit_reviewer.py`
  - 除 `audit_result.json` 外，新增 `audit_advice.json`
  - 输出：`fix_actions`、`tool_params.image_pipeline_request`、`submit_policy`
- 已实现“素材审核 -> 处理参数 -> 图片处理 -> 成品审核 -> 审核建议 -> submit payload”闭环。

## 效率优先版收口（09:44）
- 新增总控脚本：`skills/rrz-publish/scripts/rrz_review_orchestrator.py`
  - 一条命令串起：`raw_auditor -> image_pipeline_runner -> audit_reviewer -> build_submit_payload`
  - 支持：`--submit` / `--save-draft` / `--block-on-warning` / `--force`
- 新增缓存文件：`products/<product_key>/.review_cache.json`
  - `raw_auditor` 按 `raw/` 图片 hash + `product_info.json` hash 跳步
  - `image_pipeline_runner` 按 `raw/` 图片 hash + `image_pipeline_request.json` hash 跳步
  - `audit_reviewer` 按 `upload_data.json` + `image_pipeline_result.json` + `raw_audit_result.json` hash 跳步
  - `build_submit_payload` 按 `audit_result.json` + `image_pipeline_result.json` + `upload_data.json` + submit 参数跳步
- 用 `MDO3052 --submit` 连跑两次验证：
  - 第一次正常执行
  - 第二次四步全部 `cache-hit`
- 新增协议文档：`projects/rrz-review-workflow.md`
  - 目的：后续讨论直接引用流程，不再反复口头解释，减少 token 消耗

## 步进式验证基线固化（09:55）
- 新增基线文档：`projects/rrz-regression-baseline.md`
  - 固定样板单：`MDO3052`
  - 固定 L0-L4 五层验证模型
  - 明确不同脚本改动对应的最小回归范围
- 新增回归脚本：`skills/rrz-publish/scripts/rrz_regression_check.py`
  - 作用：机械验证文件链 / gate 链基线是否仍然成立
- 实跑回归通过，当前已有可重复执行的基线。

## desc 补足逻辑修复（13:48）
- 修复 `raw_auditor.py`
  - `recommended_desc_images` 不再混入低分辨率图
  - 描述图不足时，优先复用高质量主图，而不是把低质图塞进推荐集
- 修复 `image_pipeline_runner.py`
  - `drop_sources` 先过滤，再计算显式 `main/desc`
  - 显式 `desc_images` 因 drop/坏图减少后，自动从剩余素材补足
  - 若仍不足，再按主图回退补足到目标 `desc_count`
- 强制全跑 `MDO3052 --submit --force` 后结果：
  - `desc_images` 从 2 张稳定补到 3 张
  - `request_used.desc_images` 已收敛为：`mdo3052_2.jpg / mdo3052_3.png / mdo3052_4.png`
  - `raw_audit_result.recommended_desc_images` 与实际处理链一致
  - `audit_result.score` 从 92 提升到 96
  - 当前仅剩 warning：`INVALID_SOURCE_IMAGES`（坏素材 `mdo3052_official.jpg`）
