# HEARTBEAT.md

## 心跳执行规则（最高优先级）
- 总预算控制在 30 秒内
- 同一命令失败 1 次就汇报，不反复重试
- 遇到超时、权限拦截、命令不存在：立即汇报阻塞原因
- 没有异常时回复 `HEARTBEAT_OK`
- 不做长任务；超过 2 分钟的任务转 cron 或子代理

## 每次心跳仅做轻检查
1. `openclaw status` 或等价轻量状态检查
2. 如需服务自检，只做一次轻量命令，不跟随深挖
3. 不读取长历史，不重复注入旧对话，不做重型遍历

## 仅在明确需要时再做
- 天气 / 日历 / 邮件
- 子代理进度检查
- 周报、清理、memory flush
